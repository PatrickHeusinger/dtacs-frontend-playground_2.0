name: Cron Test Workflow

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Timestamp
        run: echo "Job runs at $(date -u '+%Y-%m-%d %H:%M:%S UTC')"


