name: Cron & Manuell Trigger

on:
  schedule:
    - cron: '0 2 * * *'    # runs daily 2:00 UTC
    - cron: '30 14 * * 1'  # runs monday 14:30 UTC
  workflow_dispatch:        # runs manually

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Zeit
        run: echo "Job läuft um $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
